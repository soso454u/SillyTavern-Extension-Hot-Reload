# Changelog

## 1.5.5 - 2026-08-17

- 修复 TauriTavern 在 `APP_READY` 后才激活第三方扩展、重启恢复却再次等待同一事件而固定空等 15 秒的问题；TauriTavern 现在立即进入恢复流程。
- 将普通环境的恢复兜底从 15 秒缩短到 6 秒，晚挂载界面恢复上限从 6 秒缩短到 4 秒。
- 无感重启由 `location.reload()` 改为带一次性 `st_hot_restart` 令牌的同源 `location.replace()`，强制 WebView 请求新文档；新页面启动后自动清理令牌。
- 删除流程接入 Runtime Supervisor：没有 manifest cleanup hook、但可逆运行资源已被完整追踪且源码审计安全的扩展，现在可直接清理监听器、计时器、Observer、DOM 和持久连接后热删除。
- 显式 cleanup hook 失败时也可回退到 Supervisor 托管清理；删除完成后释放对应运行时账本。
- 新增重启 URL、Tauri 恢复路径和托管热删除测试，并同步升级 manifest、包版本和内部模块令牌到 `1.5.5`。

## 1.5.4 - 2026-08-14

- 接管 SillyTavern / TauriTavern 原生“Update all / 更新全部”按钮，绕开其在执行更新前就设置 `requiresReload`、导致全部失败后仍刷新页面的路径。
- 严格校验更新接口的 `isUpToDate` 结果，并兼容 Tauri/Rust 风格的 snake_case 回包；含糊或损坏的回包按失败处理，不再误判为已更新。
- 只有更新接口确认文件变化且新版 manifest 已成功读取后，才允许自动无感重启；无法验证的结果会明确警告并保持当前页面。
- 批量摘要单独统计“结果未验证”，不会把这类项目加入重启名单。
- 补充 TauriTavern 安装目录排查说明；本插件的正式文件夹名为 `SillyTavern-Extension-Hot-Reload`。
- 新增更新结果、重启资格和原生 Update All 接管测试，并同步升级 manifest、包版本和内部模块令牌到 `1.5.4`。

## 1.5.3 - 2026-08-13

- “智能热更新全部”优先挂载到官方 `.extensions_toolbar`，找不到时自动识别扩展管理弹窗中的 Update All 操作栏。
- 兼容 SillyTavern 1.12.13 将管理按钮放在 `.popup-controls` 的旧结构，以及移除或改名工具栏 class 的主题和第三方分支。
- 旧结构回退严格限定在包含 `.extensions_info` 的同一弹窗，并校验 Update All 的 i18n/可见标签，避免误插入其他按钮组。
- 新增工具栏标签识别测试，并同步升级 manifest、包版本和内部模块令牌到 `1.5.3`。

## 1.5.2 - 2026-08-12

- 修复 SillyTavern 1.17 等未导出 `getExtensionManifest` 的版本会在模块解析阶段直接拒绝加载插件、导致设置面板始终不出现的问题。
- 将扩展 API 改为兼容式命名空间访问；原生 manifest 查询不可用时，自动从扩展目录读取 `manifest.json`。
- 在设置面板右下角显示当前插件版本，并同步升级 manifest、包版本和内部模块令牌到 `1.5.2`。

## 1.5.1 - 2026-08-12

- 声明并实现 SillyTavern 官方 `install` 生命周期钩子，让首次安装完成后立即初始化插件，无需手动刷新页面。
- 设置容器晚于插件入口挂载时，自动在后续 DOM 变更中补挂载设置抽屉；临时找不到容器时不再先移除已有面板。
- 同步升级 manifest、包版本和内部模块令牌到 `1.5.1`。

## 1.5.0 - 2026-08-12

- 新增 Runtime Supervisor，在其他第三方扩展启动前安装保守的可逆资源追踪层。
- 通过调用栈与回调上下文归属 SillyTavern 事件、DOM/jQuery 监听器、计时器、animation frame、Observer、未完成的 fetch、Worker/Socket 和新插入 DOM 节点。
- 无 cleanup hook 的扩展在追踪完整、资源账本非空、无不透明操作且新旧入口风险审计均通过时，可自动托管清理并原地换新。
- 新增全局赋值、Prototype 修改、既有 DOM 修改、动态代码、外部注册表与特殊媒体运行时风险扫描；无法证明安全时继续无感重启。
- 支持 `extension_hot_reload.self_managed_modules` 声明，允许扩展在自行换新全部本地模块和资源 URL 时绕过通用多文件缓存保护。
- 保留 Native cleanup 最高优先级、manifest 结构保护、模块缓存保护和 Git 后失败自动无感重启；不 patch `Promise.prototype`。
- 将 `loading_order` 提前到 `-1000000`，确保完整覆盖后续常规扩展的初始化阶段。
- 新增 Supervisor 单元测试，并同步升级 manifest、包版本和内部模块令牌到 `1.5.0`。

## 1.4.0 - 2026-08-12

- 将扩展管理器的全局 DOM 观察收紧为相关节点过滤，并通过 `requestAnimationFrame` 合并 UI 修复。
- 移除无感重启恢复阶段的全页面属性观察，只对待恢复抽屉、详情区和滚动容器的相关挂载做帧合并处理。
- Git 更新完成后，manifest 检查、旧运行时清理、新模块导入或激活失败时不再留下半卸载页面，而是自动降级到无感重启。
- 按钮接管选择器收紧到 SillyTavern 官方扩展管理器区域，避免误接管其他插件的同名按钮。
- 检测 `js`、`hooks`、依赖和加载顺序等 manifest 运行结构变化，需要时通过无感重启让 SillyTavern 重建生命周期状态。
- 更新与删除 Git 请求增加 120 秒超时，manifest 与入口检查增加 15 秒超时，避免单个仓库拖死批量队列。
- Cache Storage 中的目标资源改为并发删除；重启前资源预热限定为 1.25 秒，超时立即继续重启。
- 子模块安全检查新增 `new URL(..., import.meta.url)`、`Worker` 和 `SharedWorker` 本地运行资源识别。
- 同步升级 manifest、包版本和 `core.js` 导入令牌到 `1.4.0`。

## 1.3.1 - 2026-08-02

- 修复入口模块更新成功但本地子模块仍复用旧缓存时产生的“假成功”。
- 自动扫描静态导入、再导出和字符串形式的动态导入；发现扩展内子模块后改走无感重启。
- Git 更新后清除目标扩展在 Cache Storage 中的旧资源，不影响其他扩展和用户数据。
- 需要重启时不再调用旧模块的 `update` 钩子，并显示准确的模块缓存原因。
- 同步升级 manifest、包版本和 `core.js` 导入令牌到 `1.3.1`。

## 1.3.0 - 2026-08-01

- 移除扩展设置标题前的火焰图标。
- 接管 SillyTavern 扩展管理器的删除按钮，并保留原生确认与可选数据清理。
- 删除时照常调用官方 `delete` 钩子；支持 `hot_reload`、`unload` 或 `disable` 的扩展可直接从当前页面卸载，无需刷新。
- 没有安全卸载钩子的活动脚本在删除后自动执行保留现场的无感重启。
- 删除后立即移除目标扩展的样式和管理器列表项，并对失败的删除尝试恢复运行时。

## 1.2.0 - 2026-08-01

- 将无感重启前的固定等待从 450ms 缩短到 40ms，并在重启前预热新版 JavaScript/CSS 入口资源。
- 已展开抽屉不再依赖元素 `id`，现在也可按标准标题和出现顺序恢复。
- 在 `APP_READY` 后持续等待最多 6 秒，恢复由第三方扩展晚挂载的设置面板。
- 保存并恢复带 `id` 的可滚动扩展面板位置。
- 用户开始点击、滚动、触摸或键盘操作后停止延迟恢复，避免界面突然跳动。

## 1.1.1 - 2026-07-23

- 将设置面板改为 SillyTavern 标准 `inline-drawer` 结构。
- 标题栏背景、边框、颜色、圆角和箭头动画现在完全跟随当前主题。
- 无感重启现在也会保存并恢复标准扩展抽屉的展开状态。

## 1.1.0 - 2026-07-23

- 添加无需目标扩展适配的“通用无感重启”兜底。
- 重启前保存输入草稿、光标选区、聊天消息锚点、滚动位置和展开状态。
- 在 SillyTavern `APP_READY` 后自动恢复现场。
- 检测正在进行的生成任务，并延迟到生成完成或停止后再重启。
- 批量更新只执行一次无感重启，不会为每个不兼容扩展分别刷新。
- 添加重启过渡遮罩和设置开关。

## 1.0.0 - 2026-07-23

- 接管 SillyTavern 扩展管理器中的单个更新按钮。
- 添加“智能热更新全部”工具栏按钮。
- 添加安全 CSS 热替换。
- 添加基于 manifest 生命周期 hook 的 JavaScript 热载入。
- 添加安全模式和实验性强制模式。
- 添加更新失败恢复、5 秒 hook 超时和禁用扩展保护。
- 添加扩展作者热更新协议与 Node 核心测试。
