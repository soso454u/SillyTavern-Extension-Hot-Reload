{
  "name": "sillytavern-extension-hot-reload",
  "version": "1.5.3",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check index.js && node --check lib/core.js && node --check lib/runtime-supervisor.js"
  },
  "engines": {
    "node": ">=18"
  }
}
