{
  "name": "sillytavern-extension-hot-reload",
  "version": "1.1.1",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check index.js && node --check lib/core.js"
  },
  "engines": {
    "node": ">=18"
  }
}
